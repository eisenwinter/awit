---
id: AWIT-0NN06ZDN
title: 'skill: drop the generated-by footer from driving-awit'
brief: >-
  Remove the trailing generated-by-awit-init note from the driving-awit body asset and regenerate the committed skill copy.
status: closed
deps: []
labels: [phase6, p2]
refs_base: repo
refs: [.awit/comments/AWIT-0NN06ZDN/20260920T195659Z-jan.md]
assignee: agent/orchestrator
---
## Summary

The trailing italic footer in the `driving-awit` skill (`_This file is generated by awit init..._`) carries no operational value for agents and wastes prompt tokens on every injection. Delete it from the source asset and regenerate the committed copy so the renderer parity test still passes.

## Context (read first)

- `AGENTS.md`; guide §§1, 5.
- `internal/skill/assets/driving-awit.body.md:180-184` — last content bullet at 180, then blank / `---` / blank / footer note at 184.
- `.omp/skills/driving-awit/SKILL.md:187-188` — generated mirror; never hand-edit, regenerate via the existing renderer.
- `internal/skill/skill.go` — Targets, Detect, Render; find how the committed copy is regenerated and which test pins it (`TestDogfoodOmpCopyMatchesRenderer`, `TestRenderIsDeterministic`).

## Files

- Modify `internal/skill/assets/driving-awit.body.md` (delete the trailing `---` separator and footer note; line 180's anti-pattern bullet becomes the last line, file ends with a single trailing newline).
- Regenerate `.omp/skills/driving-awit/SKILL.md` through the existing renderer.
- No other files. No behavior change; no test-output wording depends on the footer (verified: no test references its text).

## Steps

- [ ] Read `internal/skill/skill.go` and the renderer/parity tests; confirm the regen command.
- [ ] Delete the footer (`---` + note) from the body asset.
- [ ] Regenerate the committed skill copy; diff to confirm the only change is the footer removal in both files.
- [ ] Run `go test ./internal/skill -count=1` to green. Submit scoped proof for orchestrator review/commit, not project-wide validation.

## Acceptance Criteria

- `go test ./internal/skill -count=1` passes, including the dogfood parity test.
- `git diff` shows only the footer-block removal in the two skill files, nothing else.
- `awit validate` prints PASS.

## Out of scope

Rewording skill content; changing renderer logic; touching seeded target dirs beyond the committed copy.
